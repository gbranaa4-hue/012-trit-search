"""tritkit.bench -- the honest benchmark suite.

Each entry reports where ternary works AND where it doesn't, on real tasks,
using the public tritkit API only. The suite IS the differentiator: a quant
library that tells you its boundaries instead of only its wins.
"""
